version: '3'
services:
  kotoba:
    build: .
    restart: always
    volumes:
      - ./logs:/var/app/logs
      - ./save_data:/var/app/save_data
      - ./persistence:/var/app/persistence
      - ./latest_log:/var/app/latest_log
